global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Nuevo trabajo para monitorear tu aplicación NestJS
  - job_name: "nest-app"
    static_configs:
      - targets: ["nest_app:3002"]
